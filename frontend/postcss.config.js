// frontend/postcss.config.js
export default {
    plugins: {
      // Use the key matching the package name we just installed
      '@tailwindcss/postcss': {},
      // Keep autoprefixer as it's standard
      'autoprefixer': {},
    },
  }